# Copy to .env and fill in your values

# Ed25519 signing key (PEM format) — generated automatically in dev if not set
SIGNING_PRIVATE_KEY=

# Anthropic API key for the explain service
# Without this, the explain service uses deterministic templates (no LLM)
ANTHROPIC_API_KEY=

# Override data directory for ingest service
DATA_DIR=.data
